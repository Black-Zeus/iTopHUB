import { Panel, PanelHeader } from "../../components/Panel";

export function UsersPage() {
  return (
    <Panel>
      <PanelHeader eyebrow="Administración" title="Usuarios del Sistema" />
      <p className="text-sm text-[var(--text-muted)]">Módulo en construcción.</p>
    </Panel>
  );
}