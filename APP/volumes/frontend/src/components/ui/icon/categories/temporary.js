// src/components/ui/icon/core/categories/temporary.js
// IMPORTANTE: Este archivo es para iconos que se están probando
// o que aún no tienen una categoría definitiva.
// Periódicamente, revisar y mover a su categoría correspondiente.

import {
    // Iconos nuevos que se están implementando
    FaBars,
    FaLayerGroup,
    FaTableCellsLarge,
} from "react-icons/fa6";

// Objeto con todos los iconos temporales
export const temporaryIcons = {
    // Menús
    bars: FaBars,
    layerGroup: FaLayerGroup,

    // Compatibilidad
    FaBars,
    FaLayerGroup,

    layout: FaTableCellsLarge,
    FaTableCellsLarge
};